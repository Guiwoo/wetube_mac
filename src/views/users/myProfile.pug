extends ../base.pug
include ../mixins/video

block content
    div.porfile__data 
        img(src="/"+user.avatarUrl).avatar
        h3=user.name
    div.video-grid 
        each video in videos.reverse()
            +video(video)
        else 
            span.empty__message No video founds
            